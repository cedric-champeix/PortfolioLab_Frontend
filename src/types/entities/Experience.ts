export interface Experience {
  id: string,
  title: string,
  company: string,
  logoURL?: string,
  description: string,
  startDate: string,
  endDate: string,
  resumeId: string,
  userId: string
}